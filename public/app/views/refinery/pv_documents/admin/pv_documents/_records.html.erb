<%= render 'refinery/admin/search_header', :url => refinery.pv_documents_admin_pv_documents_path %>
<div class='pagination_container'>
  <% if @pv_documents.any? %>
    <%= render 'pv_documents' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
