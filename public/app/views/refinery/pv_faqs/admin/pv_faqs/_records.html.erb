<%= render 'refinery/admin/search_header', :url => refinery.pv_faqs_admin_pv_faqs_path %>
<div class='pagination_container'>
  <% if @pv_faqs.any? %>
    <%= render 'pv_faqs' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
