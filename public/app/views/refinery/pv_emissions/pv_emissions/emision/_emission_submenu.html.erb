<%Refinery::PvCategories::PvCategory.where(:type_name => :emision).each do |category|%>

	<%= link_to category.name, refinery.emisiones_path(:category => category.id), :class=> "c_green_2 bolder block pad_tb_1 relative list_icon_link" %>

<% end %>
