<%= render 'refinery/admin/search_header', :url => refinery.pv_bulletins_admin_pv_bulletins_path %>
<div class='pagination_container'>
  <% if @pv_bulletins.any? %>
    <%= render 'pv_bulletins' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
